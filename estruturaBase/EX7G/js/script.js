let valorA = parseInt(prompt("Digite o 1° número: "))
let valorB = parseInt(prompt("Digite o 2° número: "))
let valorC = parseInt(prompt("Digite o 3° número: "))
let valorD = parseInt(prompt("Digite o 4° número: "))

let soma1 = valorA + valorB
let soma2 = valorA + valorC
let soma3 = valorA + valorD
let soma4 = valorB + valorC
let soma5 = valorB + valorC
let soma6 = valorC + valorD

let multiplicacao1 = valorA * valorB
let multiplicacao2 = valorA * valorC
let multiplicacao3 = valorA * valorD
let multiplicacao4 = valorB * valorC
let multiplicacao5 = valorB * valorC
let multiplicacao6 = valorC * valorD

alert("Resultado da soma de 1° valor + 2° valor = " + soma1 +
      "\nResultado da soma de 1° valor + 3° valor = " + soma2 +
      "\nResultado da soma de 1° valor + 4° valor = " + soma3 +
      "\nResultado da soma de 2° valor + 3° valor = " + soma4 + 
      "\nResultado da soma de 2° valor + 4° valor = " + soma5 +
      "\nResultado da soma de 3° valor + 4° valor = " + soma6 +
      "\nResultado da multiplicação de 1° valor + 2° valor = " + multiplicacao1 +
      "\nResultado da multiplicação de 1° valor + 3° valor = " + multiplicacao2 +
      "\nResultado da multiplicação de 1° valor + 4° valor = " + multiplicacao3 +
      "\nResultado da multiplicação de 2° valor + 3° valor = " + multiplicacao4 + 
      "\nResultado da multiplicação de 2° valor + 4° valor = " + multiplicacao5 +
      "\nResultado da multiplicação de 3° valor + 4° valor = " + multiplicacao6 
)